const RPC=process.env.SOLANA_RPC_URL||'https://api.mainnet-beta.solana.com';const PUMP='6EF8rrecthR5Dkzon8Nwu78hRvfCKubJ14M5uBEwF6P';export const dynamic='force-dynamic';export const runtime='edge';
async function rpc(method:string,params:any[]){const r=await fetch(RPC,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({jsonrpc:'2.0',id:1,method,params}),cache:'no-store'});if(!r.ok)throw Error(`RPC ${r.status}`);const j=await r.json() as any;if(j.error)throw Error(j.error.message||'RPC error');return j.result}
async function probe(url:string,init:RequestInit={}){const c=new AbortController(),t=setTimeout(()=>c.abort(),5000);try{const r=await fetch(url,{...init,signal:c.signal,cache:'no-store'});return{ok:r.ok,status:r.status}}catch(e){return{ok:false,error:String(e)}}finally{clearTimeout(t)}}
const wsCount=()=>((process.env.SOLANA_WS_URLS||process.env.SOLANA_WS_URL||'').split(',').map(x=>x.trim()).filter(Boolean).length||2);
export async function GET(){const started=Date.now(),key=process.env.SOLAMI_API_KEY||'',base=process.env.SOLAMI_DATA_WS_URL||'wss://ws.solami.dev/data/subscribe?chain=solana';let rpcState:any,solami:any={configured:!!key};try{const [slot,sigs]=await Promise.all([rpc('getSlot',[{commitment:'confirmed'}]),rpc('getSignaturesForAddress',[PUMP,{limit:1,commitment:'confirmed'}])]);rpcState={ok:true,slot,latestPumpSignature:sigs[0]?.signature||null,latestPumpBlockTime:sigs[0]?.blockTime||null}}catch(e){rpcState={ok:false,error:String(e)}}if(key){try{const u=new URL(base);if(!u.searchParams.has('api_key'))u.searchParams.set('api_key',key);solami={configured:true,endpoint:u.origin+u.pathname,upgrade:await probe(u.toString(),{headers:{Upgrade:'websocket',Connection:'Upgrade'}})}}catch(e){solami={configured:true,error:String(e)}}}const dex=await probe('https://api.dexscreener.com/tokens/v1/solana/So11111111111111111111111111111111111111112');return Response.json({status:rpcState.ok&&solami.configured&&solami.upgrade?.ok&&dex.ok?'READY':'DEGRADED',latencyMs:Date.now()-started,rpc:rpcState,solami, dex:{ok:dex.ok,status:dex.status},providers:{solanaRpc:!!process.env.SOLANA_RPC_URL,solamiApiKey:!!key,solanaWsConfigured:!!process.env.SOLANA_WS_URLS||!!process.env.SOLANA_WS_URL,dexscreener:true},realtime:{primary:key?'solami-blur':'solana-pubsub',fallbacks:wsCount(),truth:'LIVE requires an actual normalized event; connection alone is not LIVE'}},{headers:{'cache-control':'no-store'}})}